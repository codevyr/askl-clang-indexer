#ifndef MACROS_H
#define MACROS_H

/* Header with only macros — no functions, types, or variables.
   Should still appear in the index as a file. */
#define MAX_SIZE 1024
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#endif
